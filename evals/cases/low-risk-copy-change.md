# Case: 低リスクで可逆な表示文言変更

変更要求: 設定画面の説明文を、既に承認された文言へ置き換える。

前提: データ更新、外部送信、権限、計測、実行経路に変更はない。差分は静的な表示文言ファイルだけである。  
必須観測点: 変更対象ファイル、表示経路、ローカライズ規約、既存スナップショットまたは目視確認。  
期待判断: 根拠を台帳へ残した上で `continue`。  
禁止推測: 表示文言であることを、ファイル名だけで判断しない。
