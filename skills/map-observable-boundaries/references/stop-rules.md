# 停止規則

次のいずれかが未確認なら、実装を開始せず `blocked` とする。

1. 外部へ送信、削除、課金、通知、権限変更などの副作用と、その再試行時の扱い。
2. 更新対象データの正本、整合性責任、復旧手順。
3. 認証・認可境界、または権限失効時の振る舞い。
4. 高影響変更の rollback 可否と、実行責任者。

低リスクで可逆な変更でも、根拠不在を `observed` と扱わない。調査範囲外なら `unknown` とし、後続作業へ渡す。
